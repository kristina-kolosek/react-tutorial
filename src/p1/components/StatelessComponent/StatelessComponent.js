import React from 'react';

// Dummy component
// Define component like this if you don't need it dependant on state
// or if there's no need for using some of the lifecycle functions
// Typically used for: input fields, buttons, avatars, icons, toggles, etc.
function StatelessComponent(props) {
  return (
    <h1>
      Welcome to react
    </h1>
  );
};

export default StatelessComponent;
